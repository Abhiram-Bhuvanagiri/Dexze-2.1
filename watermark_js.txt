{
  "Description": "Update JavaScript to support watermark and scroll reveal",
  "Instruction": "Add the watermark updating logic inside setActiveStage, and add the IntersectionObserver logic at the end of the script for the scroll reveal effect.",
  "ReplacementChunks": [
    {
      "AllowMultiple": false,
      "EndLine": 1100,
      "ReplacementContent": "                            const imgWrapper = document.querySelector('.ideas-images-wrapper');\n                            const watermark = document.getElementById('idea-watermark');\n                            \n                            function setActiveStage(index) {\n                                listItems.forEach(item => item.classList.remove('active'));\n                                images.forEach(img => img.classList.remove('active'));\n                                texts.forEach(txt => txt.classList.remove('active'));\n                                \n                                listItems[index].classList.add('active');\n                                images[index].classList.add('active');\n                                texts[index].classList.add('active');\n                                \n                                if (watermark) {\n                                    watermark.style.opacity = 0;\n                                    setTimeout(() => {\n                                        watermark.innerText = '0' + (index + 1);\n                                        watermark.style.opacity = 1;\n                                    }, 200);\n                                }\n                            }",
      "StartLine": 1088,
      "TargetContent": "                            const imgWrapper = document.querySelector('.ideas-images-wrapper');\n                            \n                            function setActiveStage(index) {\n                                listItems.forEach(item => item.classList.remove('active'));\n                                images.forEach(img => img.classList.remove('active'));\n                                texts.forEach(txt => txt.classList.remove('active'));\n                                \n                                listItems[index].classList.add('active');\n                                images[index].classList.add('active');\n                                texts[index].classList.add('active');\n                            }"
    },
    {
      "AllowMultiple": false,
      "EndLine": 1140,
      "ReplacementContent": "                                imgWrapper.addEventListener('mouseleave', () => {\n                                    const activeImg = imgWrapper.querySelector('.idea-image.active');\n                                    if (activeImg) {\n                                        activeImg.style.transition = 'opacity 0.6s cubic-bezier(0.4, 0, 0.2, 1), transform 0.6s cubic-bezier(0.4, 0, 0.2, 1)';\n                                        activeImg.style.transform = 'scale(1) translate(0, 0)';\n                                    }\n                                });\n                            }\n\n                            // Scroll Reveal\n                            const container = document.querySelector('.interactive-ideas-container');\n                            if (container) {\n                                container.classList.add('not-in-view');\n                                const observer = new IntersectionObserver((entries) => {\n                                    entries.forEach(entry => {\n                                        if (entry.isIntersecting) {\n                                            container.classList.remove('not-in-view');\n                                            observer.unobserve(entry.target);\n                                        }\n                                    });\n                                }, { threshold: 0.15 });\n                                observer.observe(container);\n                            }\n                        });\n                    </script>",
      "StartLine": 1132,
      "TargetContent": "                                imgWrapper.addEventListener('mouseleave', () => {\n                                    const activeImg = imgWrapper.querySelector('.idea-image.active');\n                                    if (activeImg) {\n                                        activeImg.style.transition = 'opacity 0.6s cubic-bezier(0.4, 0, 0.2, 1), transform 0.6s cubic-bezier(0.4, 0, 0.2, 1)';\n                                        activeImg.style.transform = 'scale(1) translate(0, 0)';\n                                    }\n                                });\n                            }\n                        });\n                    </script>"
    }
  ],
  "TargetFile": "c:\\Users\\abhii\\OneDrive\\Desktop\\Dexze-2.1\\Dexze_9_Jun\\services-2.html",
  "toolAction": "Updating JS for watermark and scroll reveal",
  "toolSummary": "Update JS functionality"
}